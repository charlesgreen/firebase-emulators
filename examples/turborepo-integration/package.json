{
  "name": "@example/turborepo-integration",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build required for example'",
    "test": "echo 'Testing turborepo integration example...'",
    "lint": "echo 'Linting example files...'",
    "clean": "rm -rf node_modules",
    "emulators:start": "docker-compose -f docker-compose.firebase.yml up -d",
    "test:integration": "echo 'Running integration tests with Firebase emulators...'"
  }
}