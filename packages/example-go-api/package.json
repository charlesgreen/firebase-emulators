{
  "name": "@firebase-emulators/example-go-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Go example - would run with go run main.go'",
    "build": "echo 'Go example - would build with go build' && mkdir -p bin && touch bin/example-api",
    "test": "echo 'Go example - would test with go test'",
    "lint": "echo 'Go example - linting skipped (golangci-lint not installed)'",
    "format": "echo 'Go example - would format with gofmt'",
    "setup": "echo 'Go example - would setup with go mod init'",
    "clean": "rm -rf bin/ coverage/ .turbo",
    "docker:build": "echo 'Go example - would build Docker image'",
    "docker:push": "echo 'Go example - would push Docker image'"
  }
}