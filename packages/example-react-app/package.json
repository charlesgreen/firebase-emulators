{
  "name": "@firebase-emulators/example-react-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'React example - install dependencies with npm install to run'",
    "build": "echo 'React example - would build with vite build'",
    "test": "echo 'React example - would test with vitest'",
    "test:watch": "echo 'React example - would test with vitest watch'",
    "lint": "echo 'React example - linting skipped (no dependencies)'",
    "format": "echo 'React example - formatting skipped'",
    "setup": "echo 'React example - run npm install to setup'",
    "clean": "rm -rf dist node_modules .turbo",
    "preview": "echo 'React example - would preview with vite preview'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "firebase": "^10.7.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}